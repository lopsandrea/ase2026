# RoCooky

> Virtual cookbook with infinite scroll and real-time favouriting.
> Doc2Test evaluation subject (paper §4.1, "RoCooky" row).

- **Stack:** Vue 3 (CDN) + Express static server.
- **Workflow archetype:** Navigation with infinite scroll (`Navigation` row in Tab. 1).
- **License:** MIT.

```bash
npm install && npm start    # http://localhost:3000
```
