## Atomic task

{{ task | tojson(indent=2) }}

## Filtered DOM (Phase-2 output)

```html
{{ filtered_dom }}
```

A screenshot of the current page state is attached as an image input.

Generate the Selenium snippet now, honouring every hard rule.
